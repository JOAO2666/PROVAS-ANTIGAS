/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/PROVAS-ANTIGAS',
    assetPrefix: '/PROVAS-ANTIGAS/',
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig; 